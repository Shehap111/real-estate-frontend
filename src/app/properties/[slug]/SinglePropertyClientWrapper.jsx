'use client';

import React from 'react';
import SingleProperty from './SingleProperty';

const SinglePropertyClientWrapper = ({ slug }) => {
  return <SingleProperty slug={slug} />;
};

export default SinglePropertyClientWrapper;
